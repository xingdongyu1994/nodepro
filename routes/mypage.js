var express = require('express');
var router = express.Router();

/* GET users listing. */
router.get('/mypage', function(req, res, next) {
   console.log("老看京汉股份第三吞吞吐吐拖拖拖拖拖拖拖",req)
});

module.exports = router;
